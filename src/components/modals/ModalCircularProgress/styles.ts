import styled from 'styled-components/native';

export const Container = styled.View`
  align-items: center;
  justify-content: center;
`;

export const PercentageText = styled.Text`
  color: #fff;
  font-size: 28px;
  font-weight: bold;
`;

export const ProgressSubTitle = styled.Text`
  color: #fff;
  margin-top: 10px;
  font-size: 28px;
  font-weight: bold;
  text-align: center;
`;

export const CircularContentWrapper = styled.View`
  position: absolute;
  align-items: center;
  justify-content: center;
  left: 0;
  top: 0;
  bottom: 0;
  right: 0;
`;
